(defn help ()
  (print "Help for trebek"))

(def nil (list))

(defmacro while (cond body)
  (list 'if cond
    (list 'do
      body
      (list while cond body))
    'nil))

(defmacro not (cond)
  (list 'if cond false true))
